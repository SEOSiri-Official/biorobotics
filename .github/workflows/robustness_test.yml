name: Robustness Test
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Install Package and Dependencies
        run: |
          pip install --upgrade pip
          pip install pytest
          pip install -e .
      - name: Run Test Suite
        run: python -m pytest tests/test_stability.py