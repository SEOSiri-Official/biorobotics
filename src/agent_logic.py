"""
AGENT ORCHESTRATION INSTRUCTIONS:
1. When a user requests bio-robotics action:
   - Step A: Call `fetch_genomic_data` with the gene_id.
   - Step B: Extract the sequence length or complexity from the result (or use a constant for demonstration).
   - Step C: Call `resolve_biotech_spatial_intent` using the concentration derived from the genomic data.
"""
def orchestrate_lab_task(gene_id: str, plate_scale_mm: float = 100.0):
    # This is a conceptual bridge for the AI to follow
    pass